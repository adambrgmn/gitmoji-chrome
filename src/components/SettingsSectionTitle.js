import styled from 'styled-components';
import { modularScale } from 'polished';

const SettingsSectionTitle = styled.h2`
  font-size: ${modularScale(1)};
  margin-bottom: ${modularScale(-1)};
`;

export { SettingsSectionTitle as default };
