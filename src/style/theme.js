import { rgba } from 'polished';

const color = {
  bg: '#f5f5f5',
  black: '#000000',
  white: '#ffffff',
  grey: '#999999',
  yellow: '#ffdd67',
  pink: '#ff5a79',
  orange: '#ff9d44',
  shadow: rgba(162, 182, 194, 0.5),
};

const font = {
  body:
    'Avenir, -apple-system, BlinkMacSystemFont, "Segoe UI", "Roboto", "Oxygen", "Ubuntu", "Cantarell", "Fira Sans", "Droid Sans", "Helvetica Neue", sans-serif',
};

const zIndex = {
  zero: 0,
  one: 1,
  two: 2,
  three: 3,
};

export { color, font, zIndex };
